# 配置文件示例
# http_server 相关配置
"http_server":
    # 端口
    "port": "8080"

# 日志配置
log:
    # 日志文件目录
    log_dir: "./resource/logs"

# 数据库配置
database:
    # 成都 数据库
    cd:
        host: "127.0.0.1"
        port: "3306"
        user: "root"
        passwd: "root"
        dbname: "demo1"
        max_open_cons: 5
        max_idle_cons: 5
   # 重庆 数据库
    cq:
        host: "127.0.0.1"
        port: "3306"
        user: "root"
        passwd: "root"
        dbname: "demo2"
        max_open_cons: 5
        max_idle_cons: 5